[tool.ruff]
target-version = "py38"
line-length = 100

[tool.ruff.lint]
# Keep CI's policy explicit so a newer Ruff release cannot silently expand the
# default rule set. These rules cover syntax errors, Pyflakes, and import order.
select = ["E4", "E7", "E9", "F", "I"]
